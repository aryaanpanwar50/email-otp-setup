{
  "name": "email-otp-setup",
  "version": "1.0.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "bin": {
    "email-otp-setup": "dist/setup.js"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "build": "bun build ./src/index.ts ./src/setup.ts --outdir dist --target node && tsc --emitDeclarationOnly"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "nodemailer": "^7.0.9"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.2",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
